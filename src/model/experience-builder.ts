import { Job } from "./job";

export class ExperienceBuilder {
    private _jobs: Job[];
    private _projects: Project[];

    public withJob(company: string, since: Date) {
        this._jobs.push(Job.)
        return this;
    }

}